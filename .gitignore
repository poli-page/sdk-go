# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Node
node_modules/
dist/
build/
.next/
*.log
.npm/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.tox/

# PHP
vendor/
composer.lock
.phpunit.cache/

# Go
bin/
*.test
*.out

# Demo artifacts (cmd/demo writes here)
output/

# Env
.env
.env.local

# Claude Code per-machine settings (allowed-commands, etc.)
# Shared project config can still live in .claude/settings.json.
.claude/settings.local.json

# Private agent guide — references internal platform-repo paths + team
# names. Kept under .claude/ so Claude Code sessions can still open it
# manually, but never published to the public sdk-go repo.
.claude/CLAUDE.md
CLAUDE.md

# Local SDK handoff docs from the platform repo (poli-page/poli-page).
# These are derivative working copies — the canonical versions live in
# docs/onboarding/micka/ over there. Keep them out of this public repo.
sdk-engineering-guide.md
sdk-go-plan.md
sdk-roadmap.md
