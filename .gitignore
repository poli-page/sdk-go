# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Node
node_modules/
dist/
build/
.next/
*.log
.npm/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.tox/

# PHP
vendor/
composer.lock
.phpunit.cache/

# Go
bin/
*.test
*.out
# Stray binaries from `go build ./cmd/...` runs at the repo root
/docsite
/demo

# Demo artifacts (cmd/demo writes here)
output/

# Env
.env
.env.local

# Claude Code per-machine settings (allowed-commands, etc.)
# Shared project config can still live in .claude/settings.json.
.claude/settings.local.json

# Private agent guide — kept at repo root so Claude Code auto-loads it
# on session start, but gitignored so it never enters a commit. The
# content references internal platform-repo paths + team names that do
# not belong on the public sdk-go repo.
CLAUDE.md

# Local SDK handoff docs from the platform repo (poli-page/poli-page).
# These are derivative working copies — the canonical versions live in
# docs/onboarding/micka/ over there. Keep them out of this public repo.
sdk-engineering-guide.md
sdk-go-plan.md
sdk-roadmap.md
