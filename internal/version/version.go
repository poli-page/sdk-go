// Package version exposes the SDK build version, embedded into the User-Agent
// header by the transport layer.
package version

// Version is the current SDK release. Bumped manually on each release per the
// build plan (sdk-go-plan.md §12.5).
const Version = "0.9.0-rc.1"
